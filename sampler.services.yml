services:
  sampler.reporter:
    class: Drupal\sampler\Reporter
    arguments: ['@entity_type.manager', '@user.permissions', '@plugin.manager.sampler']
  plugin.manager.sampler:
    class: Drupal\sampler\SamplerPluginManager
    parent: default_plugin_manager
